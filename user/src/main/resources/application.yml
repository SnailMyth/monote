spring:
  datasource:
    primary:
      url: jdbc:mysql://localhost:3306/monote?useUnicode=true&characterEncoding=utf-8&useSSL=false
      username: root
      password: zmh456
    secondary:
       url: jdbc:mysql://localhost:3306/notes?useUnicode=true&characterEncoding=utf-8&useSSL=false
       username: root
       password: zmh456
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.springframework.boot.orm.jpa.hibernate.SpringPhysicalNamingStrategy
    properties:
      hibernate:
        dialect:org.hibernate.dialect.MySQL5Dialect



